# Auth is handled by Supabase cloud. No local containers required.
# To run a local PostgreSQL for dev DB isolation, uncomment:
#
# services:
#   postgres:
#     image: postgres:16-alpine
#     environment:
#       POSTGRES_DB: financeflow
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#     ports:
#       - "5432:5432"
